In this practical, I successfully implemented the Merge Sort algorithm in Python and analyzed its time complexity. The algorithm correctly sorted the given list by repeatedly dividing it into smaller sublists and merging them in sorted order. Merge Sort has a time complexity of O(n log n) in the best, average, and worst cases, making it efficient for sorting large datasets. However, it requires additional memory of O(n) due to the temporary arrays used during merging.